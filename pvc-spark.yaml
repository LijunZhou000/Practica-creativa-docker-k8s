apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spark-checkpoint-pvc
  namespace: practica-creativa
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi