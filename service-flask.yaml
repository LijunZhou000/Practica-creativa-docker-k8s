apiVersion: v1
kind: Service
metadata:
  name: flask
  namespace: practica-creativa
spec:
  type: NodePort
  selector:
    app: flask
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30001